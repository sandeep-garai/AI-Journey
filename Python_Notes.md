DAY 1

A class is a blueprint for a real world identity. It comprises of class variables, methods which can be static methods or regular methods.

Objects are the instance of the class, it defines the class

Constructor is the entry point of the class, it constructs the instance variable based on the values passed.

Inheritance is the property of class by which a class can inherit or acquire the properties, methods or class variable by inheriting it. It can also overwrite the methods as per its own needs.

API short for Application Programming Interface, is a feature by which the application can be invoked and get the information without understanding the underlying mechanism of it. It is mostly provided by the application for easy access.

JSON is JavaScript Object Notation, a data format in which the data is stored in key and value format. Each data has an unique key to find it. It is mostly used in webpages.

Virtual Environment is a secondary environment created to have different versions of the software for different needs. So a computer can have different environments for its different needs.

HOME-WORK
Why do we use classes instead of dictionaries?
We use classes instead of dictionaries because classes has the ability to hide the implementation details from the develope, and has various methods to store and get the value. Classes give the blueprint of the real world identity, whereas dictionaries only store the data in key-value pairs. 
The class can be used as many times as it is called through it instance.

What is an API?

What is JSON?

Why do we use virtual environment?
So the different softwares can work on different versions as per need of the software.

Differnence between == and is?
the == operator checks for the same memory location, whereas is operator checks for the same value of the variables






DAY 2

list is a data structure that can store different datatypes and can be scaled up for storage. It is mutable, can be indexed.

tuple is a data structure that is immutable, and can be indexed

dictionary is a data structure that store the data in key-value pairs and is accessed through the key, mutable

set is a data structue that is mutable, used hashing to save the data, can not be indexed, contains only the unique values.

collections.Counter is a function by which the string characters are counted.

defaultdict is a special type of dictionary that can be built to store datatypes and can be used without initialization.

deque is a data structure that has the property of adding and removing the elements from both end. 

Why are dictionaries faster than lists for searching?
Difference between list and tuple?
What is hashing?
Why is a set faster?
When would you use deque instead of a list?

dictionaries are faster than list for lookup as dictionaries used key pair to search for the value, and the data structure used hashing to store the value, hence easy to search O(n) avg.

list is mutable, indexable data structure used to store different datatype, whereas tuple is immutable, indexable data structure to store data.

hashing is a form of data storage technique where the actual data is passed through a hash table, where it got a hashing value, and based on the hash value, the storage happens.

set is faster as it use hashing for storing

deque is more useful than a list for easy inserting and removal of elements either from the front or the back. it can be easy rotated